<h2>Edit course</h2>

<%= render "form.html", changeset: @changeset,
                        action: course_path(@conn, :update, @course) %>

<%= link "View Course", to: course_path(@conn, :show, @course), class: "btn" %>
