<p id="notice"><%= notice %></p>

<h4><%= @lesson.course.title %></h4>

<h4><%= @lesson.title %></h4>

<p><%= format_markdown(@lesson.description) %></p>

<div>
  <%= @lesson.video_embed.html_safe %>
</div>

<%= @lesson.transcript %>

<ul>
  <% @lesson.course.lessons.each do |lesson| %>
    <li>
      <%= link_to lesson do %>
        <%= lesson.title %>
      <% end %>
    </li>
  <% end %>
</ul>

<% if user_signed_in? %>
    <%= link_to 'Purchase this course', new_course_purchase_path(@lesson.course) %>
<% else %>
    <li><%= link_to 'Sign in to purchase this course', signin_path %></li>
<% end %>

<%= link_to 'Back', course_path(@lesson.course) %>
